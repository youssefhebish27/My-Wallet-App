name: expense_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  fl_chart: ^1.1.1
  image_picker: ^1.2.1
  flutter_randomcolor: ^1.0.18
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  cloud_firestore: ^6.1.0
  google_sign_in: ^6.1.0
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.13.1  # تمت الإضافة هنا بشكل صحيح

flutter:
  uses-material-design: true

# --- إعدادات أيقونة التطبيق ---
flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon.png" # تأكد أن صورتك موجودة هنا